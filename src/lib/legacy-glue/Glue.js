/**
 * This is Glue's main class.
 * It is responsible for exposing the Stage, Box classes as imports.
 */

import { Stage } from './Stage.js';
import { Box } from './Box.js';

export { Stage, Box };
